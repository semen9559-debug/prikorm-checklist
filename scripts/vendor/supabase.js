/* Заглушка.
   Пока не выполнен `node tools/vendor.mjs`, клиент Supabase берётся с CDN —
   ровно как раньше. После запуска скрипта этот файл заменяется реальной
   сборкой с зафиксированной версией, и приложение перестаёт зависеть от
   стороннего хоста (см. scripts/vendor/README.md). */
export * from 'https://esm.sh/@supabase/supabase-js@2';
